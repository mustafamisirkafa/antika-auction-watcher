# Sprint 2: Security & Rate Limiting Dependencies
cryptography==41.0.7       # Fernet encryption for credentials
slowapi==0.1.9             # Rate limiting middleware
limits==3.7.0              # Rate limiting backend
python-multipart==0.0.6    # Form data parsing (for auth)
